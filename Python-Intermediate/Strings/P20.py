# Write a program to capitalize the first letter of each word in a string.
string = "riya is a good girl"

# mylist = [word.capitalize() for word in string.split()]
# print(" ".join(mylist))
print(string.title())
